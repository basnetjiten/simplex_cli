/// Created on: {{created_at}}
/// Generated with Simplex CLI

{{#use_graphql}}
// STUB: Remove this typedef after GraphQL types are generated
// EXPECTED_TYPE: G{{source_class}}Data
typedef G{{source_class}}Data = dynamic;

/// Abstract source contract for [{{source_class}}].
/// Implementations live in data/sources/ and return Ferry GXData objects.
/// Replace [G{{source_class}}Data] with the actual Ferry-generated GXData type after generation.
abstract class {{source_class}}RemoteSource {
  Future<G{{source_class}}Data?> get{{source_class}}();
}
{{/use_graphql}}
{{^use_graphql}}
import 'package:{{package_name}}/features/{{feature_name}}/data/models/{{source_name}}_model.dart';
import 'package:fpdart/fpdart.dart';

/// Abstract source contract for [{{source_class}}].
/// Implementations live in data/sources/ and call the REST API via Dio.
abstract class {{source_class}}RemoteSource {
  Future<{{source_class}}Model> get{{source_class}}();
}
{{/use_graphql}}

